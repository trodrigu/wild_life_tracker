.jumbotron.species_pic_index_jumbo
  %h1 Species Pic

= form_for(@image, html: { multipart: true, class: "dropzone"}) do |f|
  = f.hidden_field :species_id, value: @species.id
  .fallback
    = f.file_field :avatar, class: "form-control"
    %br
    = f.submit "Upload Pic"

.index_of_pics
  = render partial: 'index' 
